import type { Exercise, ExerciseVersion, PlannedSession, PlannedWorkout, SessionExerciseGroup, SessionExerciseItem, SessionSet, WorkoutSession } from '@/domain/entities';
export interface DashboardPort { countExercises(): Promise<number>; countWorkouts(): Promise<number>; getSessionsInDateRange(from: Date, to: Date): Promise<WorkoutSession[]>; getAllSessions(): Promise<WorkoutSession[]>; getLatestCompletedSession(): Promise<WorkoutSession | undefined>; getSessionEntities(ids: string[]): Promise<{ groups: SessionExerciseGroup[]; items: SessionExerciseItem[]; sets: SessionSet[] }>; getExercisesByIds(ids: string[]): Promise<Exercise[]>; getExerciseVersionsByIds(ids: string[]): Promise<ExerciseVersion[]>; getPlannedSession(id: string): Promise<PlannedSession | undefined>; getWorkout(id: string): Promise<PlannedWorkout | undefined>; }
