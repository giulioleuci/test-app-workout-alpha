import type { PlannedWorkoutStatus } from '@/domain/enums';
export type Language = 'en' | 'it' | 'es' | 'fr' | 'zh';
export interface MaintenancePort { clearUserData(categories: Set<string>): Promise<void>; resetCurrentDatabase(): Promise<void>; resetWholeApplication(): Promise<void>; loadFixtures(language: Language): Promise<void>; seedExercises(language: Language): Promise<void>; seedFullBody2x(status: PlannedWorkoutStatus, language: Language): Promise<void>; seedPPL3x(status: PlannedWorkoutStatus, language: Language): Promise<void>; seedUpperLower4x(status: PlannedWorkoutStatus, language: Language): Promise<void>; seedPowerlifting(status: PlannedWorkoutStatus, language: Language): Promise<void>; seedCalisthenics(status: PlannedWorkoutStatus, language: Language): Promise<void>; }
