import type { TemplatePort } from '@/application/templates'; import { ExerciseRepository } from '@/db/repositories/ExerciseRepository'; import { TemplateRepository } from '@/db/repositories/TemplateRepository'; import { UserProfileRepository } from '@/db/repositories/UserProfileRepository';
export const templateGateway: TemplatePort = { getAll: () => TemplateRepository.getAll(), getById: id => TemplateRepository.getById(id), add: template => TemplateRepository.add(template), update: (id, updates) => TemplateRepository.update(id, updates), delete: id => TemplateRepository.delete(id), getExercises: () => ExerciseRepository.getAll(), getRegulationProfile: () => UserProfileRepository.getRegulationProfile() };
