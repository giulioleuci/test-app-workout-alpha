import type { ActiveSessionLoadingPort } from '@/application/activeSessionLoading';
import { SessionRepository } from '@/db/repositories/SessionRepository'; import { UserProfileRepository } from '@/db/repositories/UserProfileRepository'; import { WorkoutPlanRepository } from '@/db/repositories/WorkoutPlanRepository';
export const activeSessionLoadingGateway: ActiveSessionLoadingPort = { getHydratedSession: id => SessionRepository.getHydratedSession(id), getRegulationProfile: () => UserProfileRepository.getRegulationProfile(), getPlannedSession: id => WorkoutPlanRepository.getSession(id), getPlannedWorkout: id => WorkoutPlanRepository.getWorkout(id), getPlannedGroups: ids => WorkoutPlanRepository.bulkGetGroups(ids), getPlannedItems: ids => WorkoutPlanRepository.bulkGetItems(ids), getPlannedSets: ids => WorkoutPlanRepository.getSetsByIds(ids) };
